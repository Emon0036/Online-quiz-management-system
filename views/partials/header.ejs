<header class="container hero-band reveal">
  <div class="hero-panel">
    <span class="eyebrow"><%= eyebrow || 'Quiz Platform' %></span>
    <h1 class="mt-2"><%= heading || appName %></h1>
    <% if (subheading) { %><p class="text-muted mb-0"><%= subheading %></p><% } %>
  </div>
</header>
